.project-item
  .right
    .project-item-actions
      = link_to project_path(project), method: :delete, class: 'badge' do
        %i.fa.fa-trash
    .project-item-info
      = build_status(project.last_build) unless project.last_build.nil?
      %span.color-gray-30 GitHub
  = link_to project.path, project_path(project)
%hr
